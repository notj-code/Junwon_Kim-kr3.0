/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lightroom.adobe.com', 'images.unsplash.com', 'i.ytimg.com', 'img1.daumcdn.net'],
  },
}

module.exports = nextConfig
